/** @type {import('next').NextConfig} */
const nextConfig = {
    images: {
      domains: ['cdn.prod.website-files.com'], // Add the external image domain here
    },
  };
  
  export default nextConfig;
  